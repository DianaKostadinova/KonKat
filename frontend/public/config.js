// Runtime config — ops can replace this file in the deployed container without
// rebuilding the Angular bundle. Loaded by index.html before the app bundle.
// Any value left undefined falls back to the build-time environment.
window.__KONKAT_CONFIG__ = {
  // clerkPublishableKey: 'pk_live_...',
  // apiUrl: '/api',
  // wsUrl: 'wss://example.com/ws',
};
